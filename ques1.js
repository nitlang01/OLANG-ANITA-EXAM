/*
(1.0) An array is a list of data in a square bracket. It is a means of storing data as a data structure

(1.1) .reduce(): It add up all the data in an array.
      .includes(): It checks if an element is present in an array.
      .concat(): It merges two arrays together.
      .sort(): It arranges element to the last element in an array.
      .push(): It adds an elemnt to the last element in an array.

(1.2) index 5

(1.3) ForEach returns undefined.

(1.4) var array = []
  console.log(array(1))
  */